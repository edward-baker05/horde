CompileFlags:
  # CMakeLists.txt symlinks build/compile_commands.json here on every
  # configure, so no CompilationDatabase override is needed.
  Remove:
    - "-fmodules-ts"
    - "-fmodule-mapper=*"
    - "-fdeps-format=*"
