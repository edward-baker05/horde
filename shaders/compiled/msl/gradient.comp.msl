#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_GradientUniforms
{
    float2 Resolution;
    float Time;
    float Padding;
};

kernel void main0(constant type_GradientUniforms& GradientUniforms [[buffer(0)]], texture2d<float, access::write> OutputTexture [[texture(0)]], uint3 gl_GlobalInvocationID [[thread_position_in_grid]])
{
    do
    {
        bool _54;
        if (gl_GlobalInvocationID.x < uint(GradientUniforms.Resolution.x))
        {
            _54 = gl_GlobalInvocationID.y >= uint(GradientUniforms.Resolution.y);
        }
        else
        {
            _54 = true;
        }
        if (_54)
        {
            break;
        }
        float2 _61 = float2(gl_GlobalInvocationID.xy) / GradientUniforms.Resolution;
        float _64 = _61.x;
        float _71 = _61.y;
        OutputTexture.write(float4(0.5 + (0.5 * sin(GradientUniforms.Time + (_64 * 6.283185482025146484375))), 0.5 + (0.5 * sin(((GradientUniforms.Time * 0.699999988079071044921875) + (_71 * 6.283185482025146484375)) + 2.094395160675048828125)), 0.5 + (0.5 * sin(((GradientUniforms.Time * 1.2999999523162841796875) + ((_64 + _71) * 3.1415927410125732421875)) + 4.18879032135009765625)), 1.0), uint2(gl_GlobalInvocationID.xy));
        break;
    } while(false);
}

